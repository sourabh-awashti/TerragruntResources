# Generated by ChatGPT - hopefully this will work as there's quite limited info available on the net and yaml example was hard to find
apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: {{.Values.name}}-ingressclass
spec:
  controller: acme.io/foo
  parameters:
    apiGroup: networking.k8s.io
    kind: Ingress
    name: {{.Values.name}}-ingress
